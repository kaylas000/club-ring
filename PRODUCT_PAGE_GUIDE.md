# 📝 Инструкция для AI: Создание страниц товаров и карточек каталога (2025, обновление)

> **ВАЖНО!** Следуй этой инструкции при каждой операции создания или восстановления товаров и карточек.

---

## ❗ Критические ошибки из практики

### 1. Использование разных ключей корзины
- **Используй** storageKeys: `{ equipment: 'nosorogEquipmentCart', cosmetic: 'nosorogCosmeticCart', pharma: 'nosorogPharmaCart' }`
- **В buyNow** всегда добавляй товар именно в соответствующий storageKey.

### 2. Структура объекта товара
- Всегда `{ id: Date.now(), name: ..., price: (число), category: ..., quantity: 1 }`
- Категорию — только в латинице: pharma, cosmetic, equipment.

### 3. Счетчик корзины
- После любого добавления товара всегда вызывай `window.cartManager.updateCartBadge();`.

### 4. Согласованность цен
- Цена на странице, в карточке и в корзине — всегда одна и та же!
- Проверяй data-price/card и buyNow параметры, саму цену.

### 5. Не затирай каталоги!
- Читай целиком файл каталога перед вставкой карточки.
- Вставляй карточку после `<div class="products-grid">`, не переписывай всё содержимое!

### 6. Изображения — латиница, без пробелов, желательно png.
- Храни картинки только в латинице и без спецсимволов — иначе не загрузится в браузере.

### 7. Правильный процент скидки
- Формула для бейджа: `((oldPrice - newPrice)/oldPrice*100)%`

### 8. Подключение функций и файлов
- Всегда подключай `<script src="js/main.js"></script>` перед </body>.
- Реализуй `showTab` и `buyNow` для каждой страницы товара — строго по эталону!
- В buyNow добавляй проверку существования ym и cartManager:
```javascript
if (window.cartManager) window.cartManager.updateCartBadge();
if (typeof ym !== 'undefined') ym(...);
```

### 9. Не забывай про event.stopPropagation / preventDefault в кнопках карточек-ссылок

---

## 📚 Эталонные шаблоны
- Страница товара: eleutherococcus.html или fish-oil.html
- Каталог: pharma.html
- JS-логика: js/main.js

---

## 🛠️ Чек-лист перед коммитом
### Страница товара
- [ ] Рабочие showTab и buyNow (по шаблону)
- [ ] Категория только pharma/cosmetic/equipment
- [ ] Все пути и data-атрибуты корректны
- [ ] Структура товара в корзине и категория соответствует CartManager
- [ ] Цена и скидка совпадают везде
- [ ] Вызовы ym, cartManager корректны

### Карточка в каталоге
- [ ] html-файл каталога прочитан полностью до модификаций
- [ ] Новая карточка — сразу после <div class="products-grid">
- [ ] Старые карточки на месте
- [ ] Все ссылки и data-атрибуты рабочие
- [ ] Правильная категория, скидка, картинка, название

---

## 🔍 Отладка
- Проверяй кнопки на сайте, содержимое localStorage (DevTools > Application)
- Ошибки JavaScript смотри в консоли
- Проверяй, что счетчик корзины обновляется и товары добавляются во все нужные storageKeys
- Если что-то не работает — смотри на структуру или имена ключей и функции добавления!

---

## 🚨 Прогноз проблем и решения
- Дублируется товар — учитывай quantity и сделай проверку на name+category
- Бейдж «SALE» отличается от реальной скидки — пересчитай формулой
- Нет функции showTab — вкладки не работают
- Метрика ломает JS — добавь проверку типа `typeof ym !== 'undefined'`
- Картинка не грузится — посмотри транслитерацию и расширение
- Всё исчезло из каталога — перезаписал файл вместо добавления

---

## 📞 Сообщение пользователю после выполнения
```
✅ Страница товара успешно создана!
🔗 [имя-файла].html
📁 Карточка добавлена: [категория].html
💰 Цена: [цена]₽ ([скидка]% SALE)
🏷️ Категория: [категория]
✨ Работа всех кнопок и корзины — проверена
```

---

**Последнее обновление:** 26.11.2025, 16:00 MSK
**Критические разделы — обновлены!**

# Версия: 2.1