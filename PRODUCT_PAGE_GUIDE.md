# 📝 Инструкция для AI: Создание страниц товаров и карточек каталога

> **ВАЖНО!** Эта инструкция для AI-ассистента. Следуй ей при получении команды «создать страницу товара», «добавить товар» или «восстановить товар».

---

## 🎯 Обзор

При создании страницы товара тебе необходимо:

1. **Создать страницу товара** (HTML-файл с полным описанием)
2. **Добавить карточку в каталог** (обновить pharma.html, cosmetic.html или equipment.html)
3. **Применить функцию «Купить сейчас»** (кнопка мгновенной покупки)

---

## 📊 Шаг 1: Создание страницы товара

### Формат имени файла:

Имя файла должно быть в **латинице**, через дефис, маленькими буквами.

**Примеры:**
- `fish-oil.html` — Рыбий жир
- `eleutherococcus.html` — Элеутерококк
- `protein-whey-gold.html` — Протеин Whey Gold
- `boxing-gloves-pro.html` — Перчатки боксерские PRO

---

### Шаблон страницы товара:

Используй **`fish-oil.html`** или **`eleutherococcus.html`** как эталонные шаблоны.

#### Ключевые элементы страницы:

1. **Meta-теги:**
   ```html
   <meta name="description" content="[ОПИСАНИЕ ТОВАРА]">
   <meta name="keywords" content="[КЛЮЧЕВЫЕ СЛОВА]">
   <title>[НАЗВАНИЕ ТОВАРА] — Спортпит RING | Пенза</title>
   ```

2. **Хлебные крошки (Breadcrumbs):**
   ```html
   <div class="product-detail-breadcrumb">
       <a href="index.html">Главная</a> / 
       <a href="shop.html">Магазин</a> / 
       <a href="[КАТЕГОРИЯ].html">[КАТЕГОРИЯ]</a> / 
       [НАЗВАНИЕ ТОВАРА]
   </div>
   ```

3. **Изображение товара:**
   ```html
   <div class="product-detail-image">
       <img src="[ИМЯ_ФАЙЛА].png" alt="[НАЗВАНИЕ]" loading="eager">
   </div>
   ```

4. **Цена и скидка:**
   ```html
   <div class="product-detail-price">
       <span class="current-price">[ЦЕНА]₽</span>
       <span class="old-price">[СТАРАЯ_ЦЕНА]₽</span>
   </div>
   ```

5. **Преимущества (список):**
   ```html
   <div class="product-detail-benefits">
       <h3>🎯 Преимущества:</h3>
       <ul>
           <li>[ПРЕИМУЩЕСТВО 1]</li>
           <li>[ПРЕИМУЩЕСТВО 2]</li>
           ...
       </ul>
   </div>
   ```

6. **Кнопки действий:**
   ```html
   <div class="product-detail-actions">
       <!-- Добавить в корзину -->
       <button class="btn-primary add-to-cart" 
               data-product="[НАЗВАНИЕ]" 
               data-price="[ЦЕНА]" 
               data-category="[КАТЕГОРИЯ]">
           <span>🛒</span> Добавить в корзину
       </button>
       
       <!-- Купить сейчас -->
       <button class="btn-secondary" 
               onclick="buyNow('[НАЗВАНИЕ]', [ЦЕНА], '[КАТЕГОРИЯ]')">
           <span>💳</span> Купить сейчас
       </button>
   </div>
   ```

7. **Вкладки (Описание, Характеристики, Применение):**
   ```html
   <div class="product-tabs">
       <div class="product-tabs-nav">
           <button class="active" onclick="showTab('description')">Описание</button>
           <button onclick="showTab('specs')">Характеристики</button>
           <button onclick="showTab('usage')">Применение</button>
       </div>
       
       <!-- Содержимое вкладок -->
   </div>
   ```

8. **JavaScript функции (КРИТИЧЕСКИ ВАЖНО!):**
   ```javascript
   <script>
   function showTab(tabName) {
       document.querySelectorAll('.product-tabs-content').forEach(tab => {
           tab.classList.remove('active');
       });
       document.querySelectorAll('.product-tabs-nav button').forEach(btn => {
           btn.classList.remove('active');
       });
       document.getElementById('tab-' + tabName).classList.add('active');
       event.target.classList.add('active');
   }
   
   // Функция для кнопки "Купить сейчас" - использует ту же систему хранения, что и CartManager
   function buyNow(productName, price, category) {
       const storageKeys = {
           equipment: 'nosorogEquipmentCart',
           cosmetic: 'nosorogCosmeticCart',
           pharma: 'nosorogPharmaCart'
       };
       
       const storageKey = storageKeys[category];
       if (!storageKey) {
           console.error('Неизвестная категория:', category);
           return;
       }
       
       // Добавляем товар в корзину
       const cart = JSON.parse(localStorage.getItem(storageKey) || '[]');
       cart.push({
           id: Date.now(),
           name: productName,
           price: price,
           category: category,
           quantity: 1
       });
       
       localStorage.setItem(storageKey, JSON.stringify(cart));
       
       // Обновляем счетчик корзины
       if (window.cartManager) {
           window.cartManager.updateCartBadge();
       }
       
       // Отправляем событие в метрику
       if (typeof ym !== 'undefined') {
           ym(105507158,'reachGoal','SHOP_BUY_NOW',{product:productName,price:price,category:category});
       }
       
       // Переходим в корзину
       window.location.href = 'cart.html';
   }
   </script>
   ```

---

## 📋 Шаг 2: Добавление карточки в каталог

### Какой каталог обновлять:

- **`pharma.html`** — спортивное питание, витамины, добавки
- **`cosmetic.html`** — косметика, гели, бальзамы
- **`equipment.html`** — боксерская экипировка

---

### Шаблон карточки товара:

```html
<a href="[ИМЯ_ФАЙЛА].html" 
   class="product-card [КАТЕГОРИЯ]" 
   data-category="[КАТЕГОРИЯ]" 
   data-status="[СТАТУС]" 
   style="text-decoration: none; color: inherit; display: block;">
   
    <!-- Бейдж -->
    <div class="product-badge [ТИП_БЕЙДЖА]">
        [ТЕКСТ_БЕЙДЖА]
    </div>
    
    <!-- Изображение -->
    <div class="product-image">
        <img src="[ИМЯ_ИЗОБРАЖЕНИЯ].png" 
             alt="[НАЗВАНИЕ ТОВАРА]" 
             loading="lazy">
    </div>
    
    <!-- Категория -->
    <div class="product-category">
        <span>[ИКОНКА]</span> [НАЗВАНИЕ_КАТЕГОРИИ]
    </div>
    
    <!-- Заголовок -->
    <h3 class="product-title">[НАЗВАНИЕ ТОВАРА]</h3>
    
    <!-- Краткое описание -->
    <p class="product-description">[КРАТКОЕ ОПИСАНИЕ]</p>
    
    <!-- Цена и кнопка -->
    <div class="product-price">
        <div>
            <span class="price">[ЦЕНА]₽</span>
            <span class="old-price">[СТАРАЯ_ЦЕНА]₽</span>
        </div>
        <button class="add-to-cart" 
                data-product="[НАЗВАНИЕ]" 
                data-price="[ЦЕНА]" 
                data-category="[КАТЕГОРИЯ]" 
                onclick="event.stopPropagation(); event.preventDefault(); 
                         addToCartFromCard('[НАЗВАНИЕ]', [ЦЕНА], '[КАТЕГОРИЯ]'); 
                         ym(105507158,'reachGoal','SHOP_ADD_TO_CART',
                         {product:'[НАЗВАНИЕ]',price:[ЦЕНА],category:'[КАТЕГОРИЯ]'}); 
                         return false;">
            <span>🛒</span> В корзину
        </button>
    </div>
</a>
```

---

### Типы бейджей:

| Тип | Класс | Текст |
|------|--------|------|
| Хит продаж | `bestseller` | `ХИТ` |
| Новинка | `new` | `NEW` |
| Скидка | `sale` | `SALE -XX%` |

---

### Категории и иконки:

| Категория | `category` | Иконка | Название |
|-----------|------------|---------|----------|
| Аптека | `pharma` | 💊 | Аптека |
| Косметика | `cosmetic` | 🧼 | Косметика |
| Экипировка | `equipment` | 🥊 | Экипировка |

---

## ⚠️ ВАЖНО: Правила обновления каталога

### 🚀 Алгоритм обновления:

1. **ПРОЧИТАТЬ** текущее содержимое каталога (pharma.html, cosmetic.html или equipment.html)
2. **НАЙТИ** секцию `<div class="products-grid">`
3. **ДОБАВИТЬ** новую карточку В НАЧАЛО списка (сразу после `<div class="products-grid">`)
4. **СОХРАНИТЬ** весь файл целиком со всеми существующими карточками

### ❌ НИКОГДА НЕ ДЕЛАЙ:

- Не заменяй весь каталог одной карточкой
- Не удаляй существующие карточки
- Не нарушай HTML-структуру (навигация, footer)
- Не добавляй карточку в конец списка

---

## 🐛 Анализ ошибок и решения

### ❌ ОШИБКА #1: Несовместимость систем хранения корзины

**Симптомы:**
- Кнопка "Купить сейчас" не добавляет товар в корзину
- Счетчик корзины не обновляется
- Товар не появляется на странице cart.html

**Причина:**
- Функция `buyNow` использовала универсальный ключ `localStorage.getItem('cart')`
- `CartManager` использует категорийные ключи:
  - `nosorogEquipmentCart`
  - `nosorogCosmeticCart`
  - `nosorogPharmaCart`

**Решение:**
```javascript
function buyNow(productName, price, category) {
    // ✅ ПРАВИЛЬНО: Используем категорийные ключи
    const storageKeys = {
        equipment: 'nosorogEquipmentCart',
        cosmetic: 'nosorogCosmeticCart',
        pharma: 'nosorogPharmaCart'
    };
    
    const storageKey = storageKeys[category];
    
    // ❌ НЕПРАВИЛЬНО: localStorage.getItem('cart')
    // ✅ ПРАВИЛЬНО:
    const cart = JSON.parse(localStorage.getItem(storageKey) || '[]');
    cart.push({
        id: Date.now(),  // Обязательно добавить id!
        name: productName,
        price: price,
        category: category,  // Обязательно добавить category!
        quantity: 1
    });
    
    localStorage.setItem(storageKey, JSON.stringify(cart));
}
```

---

### ❌ ОШИБКА #2: Неполная структура объекта товара

**Симптомы:**
- Товар добавляется, но некорректно отображается в корзине
- Ошибки в консоли браузера

**Причина:**
- Не хватает обязательных полей: `id`, `category`

**Решение:**
```javascript
// ❌ НЕПРАВИЛЬНО:
cart.push({
    name: productName,
    price: price,
    quantity: 1
});

// ✅ ПРАВИЛЬНО:
cart.push({
    id: Date.now(),           // Уникальный идентификатор
    name: productName,
    price: price,
    category: category,       // Категория товара
    quantity: 1
});
```

---

### ❌ ОШИБКА #3: Счетчик корзины не обновляется

**Симптомы:**
- Товар добавлен, но цифра в корзине не изменилась

**Причина:**
- Не вызван метод `updateCartBadge()` после добавления

**Решение:**
```javascript
function buyNow(productName, price, category) {
    // ... код добавления товара ...
    
    // ✅ ОБЯЗАТЕЛЬНО: Обновить счетчик корзины
    if (window.cartManager) {
        window.cartManager.updateCartBadge();
    }
    
    window.location.href = 'cart.html';
}
```

---

### ❌ ОШИБКА #4: Несоответствие цен

**Симптомы:**
- Цена на странице товара отличается от цены в каталоге
- Цена в корзине не совпадает с ожидаемой

**Причина:**
- При обновлении забыли синхронизировать цены

**Решение:**
- Всегда проверяй, что цена в 3 местах одинаковая:
  1. Страница товара (`product-detail-price`)
  2. Карточка каталога (`data-price` в кнопке)
  3. Функция `buyNow` (параметр price)

**Чек-лист:**
```
✅ Цена на странице товара: 450₽
✅ data-price в карточке каталога: 450
✅ buyNow('...', 450, '...') в кнопке "Купить сейчас"
```

---

### ❌ ОШИБКА #5: Замена всего каталога одной карточкой

**Симптомы:**
- После добавления нового товара в каталоге остался только он
- Все предыдущие товары исчезли

**Причина:**
- Файл каталога перезаписан вместо обновления
- Не прочитано текущее содержимое перед изменением

**Решение:**
```javascript
// ШАГ 1: ПРОЧИТАТЬ текущий каталог
const currentContent = get_file_contents({
    path: "pharma.html"
});

// ШАГ 2: НАЙТИ место для вставки
const gridStart = '<div class="products-grid">';
const insertPosition = currentContent.indexOf(gridStart) + gridStart.length;

// ШАГ 3: ВСТАВИТЬ новую карточку
const newCard = `
        <a href="[файл].html" class="product-card pharma"...>
            ...
        </a>
`;

const updatedContent = 
    currentContent.slice(0, insertPosition) + 
    newCard + 
    currentContent.slice(insertPosition);

// ШАГ 4: СОХРАНИТЬ ВЕСЬ файл
create_or_update_file({
    path: "pharma.html",
    content: updatedContent,  // ВЕСЬ ФАЙЛ!
    sha: currentFileSHA
});
```

---

### ❌ ОШИБКА #6: Yandex.Metrika не определена

**Симптомы:**
- Ошибка в консоли: "ym is not defined"
- Страница работает, но метрика не отслеживает события

**Причина:**
- Метрика загружается асинхронно и может быть не готова
- Блокировщики рекламы могут блокировать Яндекс.Метрику

**Решение:**
```javascript
// ❌ НЕПРАВИЛЬНО:
ym(105507158,'reachGoal','SHOP_BUY_NOW');

// ✅ ПРАВИЛЬНО: Проверка перед вызовом
if (typeof ym !== 'undefined') {
    ym(105507158,'reachGoal','SHOP_BUY_NOW',{product:productName,price:price,category:category});
}
```

---

### ❌ ОШИБКА #7: Путь к изображению неверный

**Симптомы:**
- Изображение не отображается (404 ошибка)
- На месте картинки пустое место или иконка

**Причина:**
- Файл изображения не существует
- Неправильный путь к изображению
- Изображение в другой папке

**Решение:**
```html
<!-- Проверь, что файл существует в репозитории -->

<!-- ✅ ПРАВИЛЬНО: файл в корне репозитория -->
<img src="fish-oil-omega3.png" alt="Рыбий жир">

<!-- ✅ ПРАВИЛЬНО: используем полный URL -->
<img src="https://raw.githubusercontent.com/kaylas000/club-ring/main/generated-image.png" alt="Элеутерококк">

<!-- ❌ НЕПРАВИЛЬНО: файл не существует -->
<img src="nonexistent-image.png" alt="Товар">
```

---

### ❌ ОШИБКА #8: Кнопка "Добавить в корзину" не работает

**Симптомы:**
- Клик по кнопке "Добавить в корзину" ничего не делает
- Нет анимации, нет уведомления

**Причина:**
- Не подключен `main.js`
- `CartManager` не инициализирован
- Неправильные data-атрибуты

**Решение:**
```html
<!-- ✅ ОБЯЗАТЕЛЬНО: Подключить main.js ПЕРЕД закрывающим </body> -->
<script src="js/main.js"></script>

<!-- ✅ ПРАВИЛЬНЫЕ data-атрибуты -->
<button class="btn-primary add-to-cart" 
        data-product="Настойка элеутерококка" 
        data-price="450" 
        data-category="pharma">
    <span>🛒</span> Добавить в корзину
</button>
```

---

## 🔮 Прогноз возможных проблем

### 🚨 ПРОБЛЕМА #1: Дублирование товара в корзине

**Когда возникнет:**
- Пользователь несколько раз нажимает "Купить сейчас"

**Текущее поведение:**
- Создаются отдельные записи с разными `id`

**Решение (если потребуется):**
```javascript
function buyNow(productName, price, category) {
    const storageKey = storageKeys[category];
    const cart = JSON.parse(localStorage.getItem(storageKey) || '[]');
    
    // Проверяем, есть ли уже такой товар
    const existingItem = cart.find(item => item.name === productName);
    
    if (existingItem) {
        existingItem.quantity++;  // Увеличиваем количество
    } else {
        cart.push({...});  // Добавляем новый
    }
    
    localStorage.setItem(storageKey, JSON.stringify(cart));
}
```

---

### 🚨 ПРОБЛЕМА #2: Конфликт имен файлов

**Когда возникнет:**
- Создаются файлы с похожими названиями
- Например: `protein.html` и `protein-whey.html`

**Решение:**
- Используй уникальные, описательные имена
- Добавляй бренд или характеристику:
  - ✅ `protein-whey-gold-2kg.html`
  - ✅ `protein-casein-night.html`
  - ❌ `protein.html` (слишком общее)

---

### 🚨 ПРОБЛЕМА #3: Битые ссылки в breadcrumbs

**Когда возникнет:**
- Неправильно указана категория в хлебных крошках

**Решение:**
```html
<!-- ✅ ПРАВИЛЬНО -->
<a href="pharma.html">Аптека</a>

<!-- ❌ НЕПРАВИЛЬНО -->
<a href="pharmacy.html">Аптека</a>  <!-- файл не существует -->
<a href="apteka.html">Аптека</a>    <!-- файл не существует -->
```

---

### 🚨 ПРОБЛЕМА #4: Неправильная категория в data-атрибутах

**Когда возникнет:**
- Опечатка в названии категории
- Использование русских букв

**Решение:**
```html
<!-- ✅ ПРАВИЛЬНО: только эти 3 значения -->
data-category="pharma"
data-category="cosmetic"
data-category="equipment"

<!-- ❌ НЕПРАВИЛЬНО -->
data-category="фарма"        <!-- кириллица -->
data-category="pharmacy"     <!-- неправильное слово -->
data-category="sportpit"     <!-- не существует -->
```

---

### 🚨 ПРОБЛЕМА #5: Отсутствие обработки событий onclick

**Когда возникнет:**
- Забыли добавить `event.stopPropagation()` и `event.preventDefault()`
- Клик по кнопке в карточке также активирует ссылку родителя

**Решение:**
```html
<!-- ✅ ПРАВИЛЬНО: для карточек-ссылок -->
<a href="product.html" class="product-card">
    <button onclick="event.stopPropagation(); event.preventDefault(); 
                     addToCartFromCard('...', 450, 'pharma'); 
                     return false;">
        В корзину
    </button>
</a>

<!-- ✅ ПРАВИЛЬНО: для обычных карточек -->
<div class="product-card">
    <button class="add-to-cart" data-product="...">
        В корзину
    </button>
</div>
```

---

### 🚨 ПРОБЛЕМА #6: Изображение не загружается

**Когда возникнет:**
- Файл изображения с кириллическими символами
- Пробелы в имени файла
- Файл не в репозитории

**Решение:**
```bash
# ✅ ПРАВИЛЬНО: латиница, дефисы
fish-oil-omega3.png
eleutherococcus-bottle.png
protein-whey-gold.png

# ❌ НЕПРАВИЛЬНО
рыбий жир.png              # кириллица
fish oil omega3.png        # пробелы
Протеин Whey Gold.PNG      # кириллица + пробелы
```

**Временное решение:**
```html
<!-- Если файл еще не загружен, используй placeholder -->
<img src="https://raw.githubusercontent.com/kaylas000/club-ring/main/generated-image.png" 
     alt="[НАЗВАНИЕ]">
```

---

### 🚨 ПРОБЛЕМА #7: Нарушена структура HTML

**Когда возникнет:**
- Копирование только части файла
- Потеря закрывающих тегов

**Симптомы:**
- Страница отображается некорректно
- CSS не применяется
- Навигация сломана

**Решение:**
- Всегда копируй ПОЛНУЮ структуру:
  ```html
  <!DOCTYPE html>
  <html lang="ru">
  <head>
      <!-- Meta, styles, scripts -->
  </head>
  <body>
      <header>...</header>
      <main>...</main>
      <footer>...</footer>
      <script src="js/main.js"></script>
      <script>
          // Локальные функции
      </script>
  </body>
  </html>
  ```

---

### 🚨 ПРОБЛЕМА #8: Забыли добавить функцию showTab

**Когда возникнет:**
- Вкладки не переключаются
- Ошибка в консоли: "showTab is not defined"

**Причина:**
- Не добавлена функция `showTab` в скрипт

**Решение:**
```javascript
// ✅ ОБЯЗАТЕЛЬНО добавить перед buyNow
function showTab(tabName) {
    document.querySelectorAll('.product-tabs-content').forEach(tab => {
        tab.classList.remove('active');
    });
    document.querySelectorAll('.product-tabs-nav button').forEach(btn => {
        btn.classList.remove('active');
    });
    document.getElementById('tab-' + tabName).classList.add('active');
    event.target.classList.add('active');
}
```

---

### 🚨 ПРОБЛЕМА #9: Карточка добавлена в конец вместо начала

**Когда возникнет:**
- Неправильно определено место вставки

**Симптомы:**
- Новый товар отображается внизу списка, а не вверху

**Решение:**
```javascript
// ❌ НЕПРАВИЛЬНО: добавление в конец
const insertPosition = currentContent.indexOf('</div>', 
    currentContent.indexOf('products-grid'));

// ✅ ПРАВИЛЬНО: добавление в начало
const gridStart = '<div class="products-grid">';
const insertPosition = currentContent.indexOf(gridStart) + gridStart.length;
```

---

### 🚨 ПРОБЛЕМА #10: Скидка рассчитана неправильно

**Когда возникнет:**
- Процент скидки в бейдже не совпадает с реальной скидкой

**Решение:**
```javascript
// Формула расчета скидки:
const discount = Math.round(((oldPrice - newPrice) / oldPrice) * 100);

// Пример:
// Старая цена: 600₽
// Новая цена: 450₽
// Скидка: ((600-450)/600)*100 = 25%
// Бейдж: "SALE -25%"
```

---

## 🛠️ Полный пример создания страницы товара

### Запрос пользователя:

> «Создай страницу товара для Креатина Monohydrate Pure»

### Действия AI:

1. ✅ **Определить имя файла:** `creatine-monohydrate-pure.html`
2. ✅ **Определить категорию:** Аптека (`pharma`)
3. ✅ **Определить цены:** Цена 1800₽, старая 2200₽, скидка -18%
4. ✅ **Создать страницу товара:** `creatine-monohydrate-pure.html`
   - Скопировать структуру из `eleutherococcus.html`
   - Заполнить все плейсхолдеры
   - Добавить функции `showTab` и `buyNow`
   - Указать правильные параметры в `buyNow('Креатин Monohydrate Pure', 1800, 'pharma')`
5. ✅ **Прочитать** текущий `pharma.html`
6. ✅ **Найти** `<div class="products-grid">`
7. ✅ **Вставить** карточку СРАЗУ ПОСЛЕ этого тега
8. ✅ **Проверить:**
   - Все старые карточки на месте
   - Новая карточка в начале
   - Ссылка href="creatine-monohydrate-pure.html"
   - data-product="Креатин Monohydrate Pure"
   - data-price="1800"
   - data-category="pharma"
9. ✅ **Сохранить** ВЕСЬ файл pharma.html
10. ✅ **Создать 2 коммита**

---

## ✅ Чек-лист перед сохранением

### Страница товара (`*.html`):

- [ ] Имя файла в латинице, через дефис
- [ ] Meta-теги заполнены (description, keywords, title)
- [ ] Хлебные крошки указывают правильную категорию
- [ ] Цена и скидка указаны правильно
- [ ] Преимущества товара перечислены (5-7 пунктов)
- [ ] Кнопка «Добавить в корзину» с data-атрибутами
- [ ] Кнопка «Купить сейчас» с правильными параметрами
- [ ] JavaScript функция `showTab` присутствует
- [ ] JavaScript функция `buyNow` с правильными storageKeys
- [ ] Функция `buyNow` обновляет счетчик корзины
- [ ] Проверка `typeof ym !== 'undefined'` перед метрикой
- [ ] Вкладки (Описание, Характеристики, Применение) заполнены
- [ ] Подключен `<script src="js/main.js"></script>`
- [ ] Структура объекта товара включает: id, name, price, category, quantity

### Карточка в каталоге:

- [ ] Каталог прочитан перед обновлением
- [ ] Карточка добавлена В НАЧАЛО списка `products-grid`
- [ ] Все существующие карточки сохранены
- [ ] Ссылка `href` ведет на правильный файл
- [ ] Цена совпадает с ценой на странице товара
- [ ] Бейдж (ХИТ, NEW, SALE) указан правильно
- [ ] Процент скидки рассчитан верно
- [ ] Категория `data-category` из списка: pharma/cosmetic/equipment
- [ ] Иконка соответствует категории
- [ ] Кнопка «В корзину" с `event.stopPropagation()` и `event.preventDefault()`
- [ ] Функция `addToCartFromCard` с 3 параметрами
- [ ] Yandex.Metrika событие указано
- [ ] Навигация и footer не изменены
- [ ] Hero-секция не изменена

---

## 📝 Коммиты

Создай **2 коммита** с понятными сообщениями:

1. **Коммит 1:** Создание страницы товара
   ```
   Создана страница товара: [Название товара] с функцией "Купить сейчас"
   ```

2. **Коммит 2:** Добавление карточки в каталог
   ```
   Добавлена карточка товара "[Название]" в каталог [Категория]
   ```

---

## 🎓 Важные напоминания

### 🔑 Ключи localStorage:
```javascript
const storageKeys = {
    equipment: 'nosorogEquipmentCart',
    cosmetic: 'nosorogCosmeticCart',
    pharma: 'nosorogPharmaCart'
};
```
**⚠️ КРИТИЧЕСКИ ВАЖНО:** Всегда используй именно эти ключи!

### 📦 Структура товара в корзине:
```javascript
{
    id: Date.now(),           // Уникальный ID
    name: "Название",         // Полное название
    price: 450,               // Цена числом (без ₽)
    category: "pharma",       // Категория (pharma/cosmetic/equipment)
    quantity: 1               // Количество
}
```

### 🎨 CSS классы кнопок:
```html
<!-- Первая кнопка (золотая) -->
<button class="btn-primary add-to-cart" ...>

<!-- Вторая кнопка (темная) -->
<button class="btn-secondary" onclick="buyNow(...)">
```

---

## 🔄 Полный алгоритм работы

### При получении команды "создать товар":

```
1. Извлечь данные:
   - Название товара
   - Категория (pharma/cosmetic/equipment)
   - Цена и старая цена
   - Описание и характеристики

2. Определить имя файла:
   - Транслитерация в латиницу
   - Замена пробелов на дефисы
   - Все маленькие буквы
   - Добавить .html

3. Создать страницу товара:
   - Скопировать шаблон из eleutherococcus.html
   - Заменить все плейсхолдеры
   - Проверить функции showTab и buyNow
   - Проверить storageKeys в buyNow
   - Проверить структуру объекта товара

4. Прочитать каталог:
   - pharma.html / cosmetic.html / equipment.html
   - Сохранить текущее содержимое

5. Добавить карточку:
   - Найти <div class="products-grid">
   - Вставить карточку СРАЗУ ПОСЛЕ этого тега
   - Проверить все data-атрибуты
   - Проверить функцию addToCartFromCard

6. Сохранить каталог:
   - ВЕСЬ файл целиком
   - Со всеми старыми карточками
   - С правильным SHA

7. Создать коммиты:
   - Коммит 1: страница товара
   - Коммит 2: карточка в каталоге

8. Сообщить пользователю:
   - ✅ Что создано
   - 🔗 Ссылки на файлы
   - 💡 Что нужно проверить
```

---

## 📞 Сообщение пользователю после выполнения

```
✅ Страница товара успешно создана!

📄 Создан файл: [имя-файла].html
🔗 Ссылка: [имя-файла].html
📁 Карточка добавлена в каталог [категория].html
💰 Цена: [цена]₽ (скидка -[процент]%)
🏷️ Категория: [категория]

✨ Функционал:
- ✅ Кнопка "Добавить в корзину"
- ✅ Кнопка "Купить сейчас"
- ✅ Интеграция с системой корзины
- ✅ Отслеживание Яндекс.Метрика

🔍 Проверьте:
- Страница товара: [имя-файла].html
- Каталог: [категория].html
- Добавление в корзину работает
```

---

## 🔧 Отладка проблем

### Если кнопка "Купить сейчас" не работает:

1. Открой консоль браузера (F12)
2. Проверь ошибки JavaScript
3. Проверь, что:
   - Функция `buyNow` определена
   - `storageKeys` правильные
   - `category` передана корректно
   - localStorage доступен

### Если товар не появляется в корзине:

1. Открой DevTools → Application → Local Storage
2. Найди ключи:
   - `nosorogEquipmentCart`
   - `nosorogCosmeticCart`
   - `nosorogPharmaCart`
3. Проверь содержимое
4. Проверь структуру объекта (id, name, price, category, quantity)

### Если счетчик корзины не обновляется:

1. Проверь, что `cartManager` инициализирован:
   ```javascript
   console.log(window.cartManager); // должен быть объект
   ```
2. Проверь вызов:
   ```javascript
   if (window.cartManager) {
       window.cartManager.updateCartBadge();
   }
   ```

---

## 🔄 Резюме

При создании страницы товара:

1. Используй **`eleutherococcus.html`** как эталон (обновленная версия с исправленной функцией `buyNow`)
2. Создай файл с именем в латинице
3. Скопируй ПОЛНУЮ HTML-структуру
4. Заполни все разделы: meta-теги, описание, характеристики, применение
5. Добавь обе кнопки: "Добавить в корзину" и "Купить сейчас"
6. Используй правильные `storageKeys` в функции `buyNow`
7. Добавь проверки на существование `ym` и `cartManager`
8. Прочитай каталог (pharma.html, cosmetic.html или equipment.html)
9. Добавь карточку в начало `<div class="products-grid">`
10. Сохрани каталог со ВСЕМИ старыми карточками
11. Сделай 2 коммита с понятными сообщениями
12. Сообщи пользователю о результате

---

## 📚 Референсы

**Эталонные файлы для копирования:**
- `eleutherococcus.html` — Страница товара (последняя версия с исправлениями)
- `fish-oil.html` — Страница товара (альтернативный пример)
- `pharma.html` — Каталог аптеки (пример карточек)

**Файлы системы:**
- `js/main.js` — CartManager и обработчики
- `cart.js` — Логика страницы корзины
- `css/style.css` — Основные стили
- `css/catalog.css` — Стили каталога

---

**Последнее обновление:** 26.11.2025, 15:50 MSK  
**Версия:** 2.0  
**Статус:** Актуально  
**Критические изменения:**  
- Добавлен анализ ошибок с решениями
- Добавлен прогноз возможных проблем
- Исправлена функция buyNow для совместимости с CartManager
- Добавлены детальные чек-листы
- Добавлен раздел отладки