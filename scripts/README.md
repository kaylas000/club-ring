# 🤖 Автоматическая генерация статей о боксе

Система автоматического парсинга новостей бокса и создания статей для сайта club-ring.ru.

## 📋 Как это работает

1. **Парсинг новостей** (`parse-news.js`)
   - Собирает новости с проверенных RSS-источников
   - Фильтрует дубликаты и нерелевантный контент
   - Сохраняет результаты в `parsed-news.json`

2. **Генерация статей** (`generate-article.js`)
   - Берет новости из `parsed-news.json`
   - Отправляет запрос в Cloudflare Worker (AI)
   - Создает HTML файл статьи по шаблону
   - Обновляет `articles-data.js` и `sitemap.xml`

3. **Автоматизация** (GitHub Actions)
   - Запускается каждый день в 10:00 UTC
   - Автоматически коммитит и пушит изменения

## 🚀 Быстрый старт

### Локальный запуск

```bash
cd scripts
npm install
node main.js
```

### Проверка парсера

```bash
cd scripts
node parse-news.js
```

### Генерация одной статьи

```bash
cd scripts
node generate-article.js
```

## 📰 Источники новостей

Настроены в `news-sources.json`:

- **BoxingScene** (boxingscene.com) - англ
- **World Boxing News** (worldboxingnews.net) - англ
- **The Ring Magazine** (ringtv.com) - англ
- **AllBoxing** (allboxing.ru) - рус
- **Vringe** (vringe.com) - рус

## ⚙️ Настройка

### Добавить новый источник

Отредактируйте `news-sources.json`:

```json
{
  "name": "Название",
  "url": "https://example.com",
  "rss": "https://example.com/feed",
  "type": "rss",
  "enabled": true,
  "language": "ru"
}
```

### Изменить фильтры

В `news-sources.json` секция `filters`:

```json
{
  "filters": {
    "minWords": 50,
    "maxWords": 2000,
    "keywords": ["бокс", "boxing"],
    "excludeKeywords": ["ufc", "mma"]
  }
}
```

## 🔧 Требования

- Node.js 20+
- Доступ к Cloudflare Worker (для AI генерации)
- GitHub Actions (для автоматизации)

## 📝 Формат статей

Статьи создаются по шаблону из `ARTICLE_TEMPLATE.md`:
- SEO-оптимизированные заголовки
- Schema.org разметка
- Адаптивный дизайн
- Интеграция с Яндекс.Метрикой

## 🐛 Отладка

Если что-то не работает:

1. Проверьте логи GitHub Actions
2. Убедитесь, что RSS фиды доступны
3. Проверьте Cloudflare Worker endpoint
4. Посмотрите `parsed-news.json` на наличие данных

## 📞 Поддержка

При проблемах проверьте:
- `parsed-news.json` - есть ли новости
- Логи Cloudflare Worker
- GitHub Actions workflow runs
