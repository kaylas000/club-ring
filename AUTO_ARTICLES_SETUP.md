# 🤖 Настройка автоматической генерации статей

## ✅ Что уже сделано

1. ✅ Создана система парсинга новостей с проверенных источников
2. ✅ Настроена генерация статей через AI (Cloudflare Worker)
3. ✅ Добавлен GitHub Actions workflow для ежедневного запуска
4. ✅ Обновлен Cloudflare Worker для поддержки генерации статей

## 🚀 Что нужно сделать сейчас

### 1. Обновить Cloudflare Worker

```bash
cd ai-backend
npx wrangler deploy
```

Это обновит Worker с новым endpoint `/generate-article`.

### 2. Установить зависимости для скриптов

```bash
cd scripts
npm install
```

### 3. Протестировать локально (опционально)

```bash
cd scripts
node parse-news.js    # Проверить парсинг
node generate-article.js  # Сгенерировать тестовую статью
```

### 4. Настроить GitHub Actions (автоматически)

GitHub Actions уже настроен и будет запускаться каждый день в 10:00 UTC (13:00 МСК).

**Проверьте:**
- Перейдите в репозиторий → вкладка **Actions**
- Убедитесь, что workflow включен
- При необходимости запустите вручную через кнопку **Run workflow**

## 📰 Источники новостей

Настроены в `scripts/news-sources.json`:

- **BoxingScene** (boxingscene.com) - англ
- **World Boxing News** (worldboxingnews.net) - англ  
- **The Ring Magazine** (ringtv.com) - англ
- **AllBoxing** (allboxing.ru) - рус
- **Vringe** (vringe.com) - рус

### Добавить свой источник

Отредактируйте `scripts/news-sources.json`:

```json
{
  "name": "Название",
  "url": "https://example.com",
  "rss": "https://example.com/feed",
  "type": "rss",
  "enabled": true,
  "language": "ru"
}
```

## 🔧 Как это работает

1. **Каждый день в 10:00 UTC:**
   - GitHub Actions запускает скрипт `scripts/main.js`
   - Парсит новости с RSS-источников
   - Фильтрует дубликаты и нерелевантный контент
   - Генерирует статью через AI (Cloudflare Worker)
   - Создает HTML файл статьи
   - Обновляет `js/articles-data.js` и `sitemap.xml`
   - Коммитит и пушит изменения в репозиторий

2. **Результат:**
   - Новая статья появляется на сайте автоматически
   - Статья видна в каталоге `articles.html`
   - Индексируется поисковиками через `sitemap.xml`

## ⚙️ Настройка фильтров

В `scripts/news-sources.json` можно настроить:

```json
{
  "filters": {
    "minWords": 50,        // Минимум слов в новости
    "maxWords": 2000,       // Максимум слов
    "keywords": ["бокс", "boxing"],  // Обязательные слова
    "excludeKeywords": ["ufc", "mma"]  // Исключающие слова
  }
}
```

## 🐛 Отладка

### Проверить парсинг новостей

```bash
cd scripts
node parse-news.js
# Результат в parsed-news.json
```

### Проверить генерацию статьи

```bash
cd scripts
node generate-article.js
# Создаст HTML файл в корне проекта
```

### Проверить GitHub Actions

1. Перейдите в репозиторий → **Actions**
2. Откройте последний workflow run
3. Посмотрите логи каждого шага

### Проверить Cloudflare Worker

```bash
curl -X POST https://club-ring-ai.club-ring-ai.workers.dev/generate-article \
  -H "Content-Type: application/json" \
  -d '{"prompt":"Напиши статью о боксе","title":"Тест"}'
```

## 📋 Формат статей

Статьи создаются по шаблону `ARTICLE_TEMPLATE.md`:
- SEO-оптимизированные заголовки
- Schema.org разметка для поисковиков
- Адаптивный дизайн
- Интеграция с Яндекс.Метрикой
- Автоматическое добавление в каталог статей

## ⚠️ Важные замечания

1. **Проверка статей:** После автоматической генерации рекомендуется проверить статью перед публикацией (хотя бы первые несколько раз)

2. **Дубликаты:** Система автоматически фильтрует дубликаты по заголовкам

3. **Ограничения:** GitHub Actions имеет лимиты на бесплатные минуты (2000 минут/месяц), но для ежедневного запуска этого достаточно

4. **RSS фиды:** Некоторые источники могут быть недоступны или изменить формат RSS - в этом случае нужно обновить `news-sources.json`

## 🎯 Следующие шаги

1. ✅ Обновить Cloudflare Worker (`npx wrangler deploy`)
2. ✅ Установить зависимости (`cd scripts && npm install`)
3. ✅ Проверить GitHub Actions (включен ли workflow)
4. ✅ Подождать первого автоматического запуска или запустить вручную

## 📞 Поддержка

При проблемах проверьте:
- Логи GitHub Actions
- Файл `scripts/parsed-news.json` (есть ли новости)
- Cloudflare Worker логи
- Формат RSS фидов источников

---

**Готово!** Система будет автоматически генерировать статьи каждый день. 🎉
