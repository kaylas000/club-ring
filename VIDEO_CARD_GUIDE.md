# 📹 Инструкция для AI: Добавление видео-карточек на главную страницу

> **ВАЖНО!** Эта инструкция создана на основе анализа критических ошибок при работе с index.html. Следуй ей точно!

---

## 🚨 КРИТИЧЕСКИЕ ОШИБКИ ИЗ ПРАКТИКИ

### ❌ ОШИБКА #1: Потеря всего содержимого файла
**Что произошло:**
- При попытке добавить видео-карточку файл index.html был сокращён с 38,640 байт до 147 байт
- Потеряны ВСЕ секции: услуги, товары, тренеры, блог, контакты, карта
- Остался только «скелет» HTML

**Причина:**
- Файл был перезаписан вместо обновления
- Не было прочитано ПОЛНОЕ содержимое перед изменением
- Использован placeholder вместо реального контента

**Решение:**
```javascript
// ✅ ПРАВИЛЬНО: ВСЕГДА читай ВЕСЬ файл целиком
const fullContent = await getFileContents({ path: "index.html", ref: "main" });

// ✅ ПРАВИЛЬНО: Найди точное место вставки
const insertMarker = '<div class="videos-grid"';
const insertPosition = fullContent.indexOf(insertMarker) + fullContent.indexOf('>', fullContent.indexOf(insertMarker)) + 1;

// ✅ ПРАВИЛЬНО: Вставь новую карточку, сохранив ВСЁ остальное
const updatedContent = 
    fullContent.slice(0, insertPosition) + 
    newVideoCard + 
    fullContent.slice(insertPosition);

// ❌ НЕПРАВИЛЬНО: Создавать новый файл с нуля
// ❌ НЕПРАВИЛЬНО: Использовать шаблоны или placeholder'ы
```

---

### ❌ ОШИБКА #2: Неправильный формат embed URL
**Что произошло:**
- Использован обычный URL видео вместо embed URL
- Видео не отображалось в iframe

**Причина:**
- Не преобразован URL из формата просмотра в формат embed

**Решение:**
```html
<!-- ❌ НЕПРАВИЛЬНО: обычный URL -->
<iframe src="https://rutube.ru/video/1f2ab7ebcfbdf63910a8295f63adc7da/"></iframe>

<!-- ✅ ПРАВИЛЬНО: embed URL -->
<iframe src="https://rutube.ru/play/embed/1f2ab7ebcfbdf63910a8295f63adc7da/"></iframe>
```

**Формула преобразования:**
```
https://rutube.ru/video/{VIDEO_ID}/?r=plwd
          ↓
https://rutube.ru/play/embed/{VIDEO_ID}/
```

---

### ❌ ОШИБКА #3: Отсутствие резервного копирования
**Что произошло:**
- После порчи файла не было быстрого способа восстановления
- Пришлось вручную восстанавливать из старых коммитов

**Причина:**
- Нет автоматического создания backup перед изменениями

**Решение:**
- Всегда проверяй наличие резервной копии
- При критических изменениях используй SHA предыдущей версии

---

## 📋 БЕЗОПАСНЫЙ АЛГОРИТМ ДОБАВЛЕНИЯ ВИДЕО-КАРТОЧКИ

### Шаг 1: Подготовка данных

**Входные данные от пользователя:**
1. URL видео с Rutube
2. Название видео
3. Описание видео
4. Тип карточки (gold/silver)

**Пример:**
```javascript
const videoData = {
    url: "https://rutube.ru/video/1f2ab7ebcfbdf63910a8295f63adc7da/?r=plwd",
    title: "Заб Джуда с юмором про бой против Цзю",
    description: "В ноябре 2001 года состоялся поединок...",
    cardType: "gold", // или "silver"
    eventName: "Judah_vs_Tszyu" // для метрики
};
```

---

### Шаг 2: Извлечение VIDEO_ID

```javascript
// Из URL вида: https://rutube.ru/video/1f2ab7ebcfbdf63910a8295f63adc7da/?r=plwd
// Нужно получить: 1f2ab7ebcfbdf63910a8295f63adc7da

function extractVideoId(url) {
    const match = url.match(/\/video\/([a-f0-9]+)/);
    if (!match) {
        throw new Error('Неверный формат URL видео');
    }
    return match[1];
}

const videoId = extractVideoId(videoData.url);
// Результат: "1f2ab7ebcfbdf63910a8295f63adc7da"
```

---

### Шаг 3: Создание embed URL

```javascript
const embedUrl = `https://rutube.ru/play/embed/${videoId}/`;
// Результат: "https://rutube.ru/play/embed/1f2ab7ebcfbdf63910a8295f63adc7da/"
```

---

### Шаг 4: Генерация HTML карточки

```javascript
function generateVideoCard(videoData, videoId, embedUrl) {
    const cardClass = videoData.cardType; // "gold" или "silver"
    const btnClass = cardClass === "gold" ? "btn-primary" : "btn-secondary";
    
    return `
    <div class="video-card ${cardClass}">
      <div class="preview" style="border-radius:11px; overflow:hidden; position:relative; padding-bottom:56.25%; height:0;">
        <iframe src="${embedUrl}" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen title="${videoData.title}" style="position:absolute; top:0; left:0; width:100%; height:100%; border:0;"></iframe>
      </div>
      <h3>${videoData.title}</h3>
      <p>${videoData.description}</p>
      <a href="${videoData.url}" class="${btnClass}" target="_blank" onclick="ym(105507158,'reachGoal','VIDEO_CLICK',{video:'${videoData.eventName}'}); return true;">Смотреть</a>
    </div>`;
}
```

---

### Шаг 5: Чтение ПОЛНОГО файла index.html

```javascript
// ✅ ОБЯЗАТЕЛЬНО: Читай ВЕСЬ файл
const currentFile = await getFileContents({
    owner: "kaylas000",
    repo: "club-ring",
    path: "index.html",
    ref: "main"
});

// ✅ ОБЯЗАТЕЛЬНО: Сохрани SHA для обновления
const currentSHA = currentFile.sha;
const fullContent = currentFile.content;

// ✅ ОБЯЗАТЕЛЬНО: Проверь размер файла
if (fullContent.length < 30000) {
    throw new Error('❌ ОШИБКА: Файл слишком маленький! Возможно, он повреждён.');
}

console.log(`✅ Файл прочитан: ${fullContent.length} символов`);
```

---

### Шаг 6: Поиск места вставки

```javascript
// ✅ ПРАВИЛЬНО: Ищем начало videos-grid
const videosGridStart = '<div class="videos-grid"';
const startIndex = fullContent.indexOf(videosGridStart);

if (startIndex === -1) {
    throw new Error('❌ ОШИБКА: Не найдена секция videos-grid!');
}

// ✅ ПРАВИЛЬНО: Находим закрывающую скобку >
const closingBracket = fullContent.indexOf('>', startIndex);
const insertPosition = closingBracket + 1;

console.log(`✅ Позиция вставки найдена: ${insertPosition}`);
```

---

### Шаг 7: Вставка новой карточки

```javascript
// ✅ ПРАВИЛЬНО: Вставляем В НАЧАЛО списка (сразу после <div class="videos-grid">)
const newVideoCard = generateVideoCard(videoData, videoId, embedUrl);

const updatedContent = 
    fullContent.slice(0, insertPosition) + 
    '\n    ' + newVideoCard + 
    fullContent.slice(insertPosition);

// ✅ ОБЯЗАТЕЛЬНО: Проверь размер результата
if (updatedContent.length < fullContent.length) {
    throw new Error('❌ ОШИБКА: Новый файл МЕНЬШЕ старого! Отменяю операцию.');
}

console.log(`✅ Старый размер: ${fullContent.length}, новый: ${updatedContent.length}`);
```

---

### Шаг 8: Сохранение изменений

```javascript
await createOrUpdateFile({
    owner: "kaylas000",
    repo: "club-ring",
    path: "index.html",
    branch: "main",
    content: updatedContent,
    sha: currentSHA, // ✅ ОБЯЗАТЕЛЬНО используй SHA!
    message: `Добавлена видео-карточка: ${videoData.title}`
});

console.log('✅ Файл успешно обновлён!');
```

---

## 🔮 ПРОГНОЗ ВОЗМОЖНЫХ ПРОБЛЕМ

### 🚨 ПРОБЛЕМА #1: Дублирование видео
**Когда возникнет:**
- Пользователь дважды попросит добавить одно и то же видео

**Решение:**
```javascript
// Проверка на дубликаты перед вставкой
if (fullContent.includes(videoId)) {
    console.warn('⚠️ ВНИМАНИЕ: Это видео уже добавлено!');
    // Спроси пользователя, добавлять ли повторно
    return;
}
```

---

### 🚨 ПРОБЛЕМА #2: Неверный URL видео
**Когда возникнет:**
- Пользователь даст URL с другого сервиса (YouTube, VK и т.д.)

**Решение:**
```javascript
function validateRutubeUrl(url) {
    if (!url.includes('rutube.ru')) {
        throw new Error('❌ ОШИБКА: Поддерживаются только видео с Rutube!');
    }
    if (!url.includes('/video/')) {
        throw new Error('❌ ОШИБКА: Неверный формат URL Rutube!');
    }
}
```

---

### 🚨 ПРОБЛЕМА #3: Слишком длинное описание
**Когда возникнет:**
- Описание видео > 300 символов ломает вёрстку

**Решение:**
```javascript
function truncateDescription(description, maxLength = 250) {
    if (description.length <= maxLength) return description;
    return description.slice(0, maxLength).trim() + '...';
}
```

---

### 🚨 ПРОБЛЕМА #4: Секция videos не существует
**Когда возникнет:**
- На странице, где нет секции videos, попытка добавить видео

**Решение:**
```javascript
if (!fullContent.includes('id="videos"')) {
    throw new Error('❌ ОШИБКА: На этой странице нет секции videos!');
}
```

---

### 🚨 ПРОБЛЕМА #5: Конфликт SHA
**Когда возникнет:**
- Файл изменился между чтением и записью

**Решение:**
```javascript
try {
    await createOrUpdateFile({...});
} catch (error) {
    if (error.message.includes('sha')) {
        console.error('❌ КОНФЛИКТ: Файл изменился! Перечитываю...');
        // Перечитай файл и попробуй снова
    }
}
```

---

## ✅ ПОЛНЫЙ ЧЕК-ЛИСТ ПЕРЕД КОММИТОМ

### Проверка входных данных:
- [ ] URL видео валидный (Rutube)
- [ ] Название < 100 символов
- [ ] Описание < 300 символов
- [ ] Тип карточки: gold или silver
- [ ] Название для метрики указано

### Проверка файла:
- [ ] index.html прочитан ПОЛНОСТЬЮ
- [ ] Размер файла > 30,000 символов
- [ ] SHA получен и сохранён
- [ ] Секция videos-grid найдена

### Проверка карточки:
- [ ] VIDEO_ID извлечён корректно
- [ ] Embed URL сформирован: `/play/embed/{ID}/`
- [ ] HTML карточки валидный
- [ ] Все кавычки экранированы

### Проверка вставки:
- [ ] Позиция вставки найдена
- [ ] Новая карточка в НАЧАЛЕ списка
- [ ] Новый размер файла БОЛЬШЕ старого
- [ ] Все старые карточки на месте

### Проверка сохранения:
- [ ] SHA указан в запросе
- [ ] Сообщение коммита понятное
- [ ] Ответ API проверен на ошибки

---

## 🎓 ПРИМЕР: Полный код добавления видео

```javascript
async function addVideoCard(videoUrl, title, description, cardType = "gold", eventName) {
    console.log('🎬 Начинаю добавление видео-карточки...');
    
    // Шаг 1: Валидация
    if (!videoUrl.includes('rutube.ru/video/')) {
        throw new Error('❌ Поддерживаются только видео с Rutube!');
    }
    
    // Шаг 2: Извлечение VIDEO_ID
    const videoId = videoUrl.match(/\/video\/([a-f0-9]+)/)[1];
    console.log(`✅ VIDEO_ID: ${videoId}`);
    
    // Шаг 3: Формирование embed URL
    const embedUrl = `https://rutube.ru/play/embed/${videoId}/`;
    
    // Шаг 4: Чтение файла
    const currentFile = await getFileContents({
        owner: "kaylas000",
        repo: "club-ring",
        path: "index.html",
        ref: "main"
    });
    
    const fullContent = currentFile.content;
    const currentSHA = currentFile.sha;
    
    // Шаг 5: Проверка размера
    if (fullContent.length < 30000) {
        throw new Error('❌ Файл повреждён!');
    }
    console.log(`✅ Файл прочитан: ${fullContent.length} символов`);
    
    // Шаг 6: Проверка дубликатов
    if (fullContent.includes(videoId)) {
        console.warn('⚠️ Видео уже добавлено!');
        return;
    }
    
    // Шаг 7: Поиск позиции вставки
    const videosGridStart = '<div class="videos-grid"';
    const startIndex = fullContent.indexOf(videosGridStart);
    if (startIndex === -1) {
        throw new Error('❌ Секция videos-grid не найдена!');
    }
    
    const closingBracket = fullContent.indexOf('>', startIndex);
    const insertPosition = closingBracket + 1;
    
    // Шаг 8: Генерация карточки
    const btnClass = cardType === "gold" ? "btn-primary" : "btn-secondary";
    const newCard = `
    <div class="video-card ${cardType}">
      <div class="preview" style="border-radius:11px; overflow:hidden; position:relative; padding-bottom:56.25%; height:0;">
        <iframe src="${embedUrl}" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen title="${title}" style="position:absolute; top:0; left:0; width:100%; height:100%; border:0;"></iframe>
      </div>
      <h3>${title}</h3>
      <p>${description}</p>
      <a href="${videoUrl}" class="${btnClass}" target="_blank" onclick="ym(105507158,'reachGoal','VIDEO_CLICK',{video:'${eventName}'}); return true;">Смотреть</a>
    </div>`;
    
    // Шаг 9: Вставка
    const updatedContent = 
        fullContent.slice(0, insertPosition) + 
        newCard + 
        fullContent.slice(insertPosition);
    
    // Шаг 10: Проверка размера результата
    if (updatedContent.length <= fullContent.length) {
        throw new Error('❌ Новый файл не больше старого!');
    }
    console.log(`✅ Новый размер: ${updatedContent.length} (+${updatedContent.length - fullContent.length})`);
    
    // Шаг 11: Сохранение
    await createOrUpdateFile({
        owner: "kaylas000",
        repo: "club-ring",
        path: "index.html",
        branch: "main",
        content: updatedContent,
        sha: currentSHA,
        message: `Добавлена видео-карточка: ${title}`
    });
    
    console.log('✅ Видео-карточка успешно добавлена!');
    return {
        videoId,
        embedUrl,
        success: true
    };
}
```

---

## 📞 СООБЩЕНИЕ ПОЛЬЗОВАТЕЛЮ

```
✅ Видео-карточка успешно добавлена!

🎬 Видео: [НАЗВАНИЕ]
🔗 Embed URL: [EMBED_URL]
📊 Позиция: В начале списка
📏 Размер файла: [СТАРЫЙ] → [НОВЫЙ] (+[РАЗНИЦА] байт)

✨ Что добавлено:
- Встроенный плеер Rutube
- Заголовок и описание
- Кнопка "Смотреть" с переходом на Rutube
- Отслеживание клика в Яндекс.Метрике

🔍 Проверьте:
- Главная страница: index.html (секция "Видео канал")
- Видео должно быть ПЕРВЫМ в списке
- Плеер должен корректно отображаться
```

---

## 🛠️ ОТЛАДКА ПРОБЛЕМ

### Если видео не отображается:
1. Проверь embed URL: должен быть `/play/embed/`
2. Проверь VIDEO_ID: 32 символа (a-f0-9)
3. Проверь атрибуты iframe: allowfullscreen, allow
4. Проверь стили: padding-bottom:56.25% для 16:9

### Если карточка не появилась:
1. Проверь размер файла: должен увеличиться
2. Проверь позицию: сразу после `<div class="videos-grid">`
3. Очисти кэш браузера (Ctrl+F5)
4. Проверь GitHub Pages (может кэшироваться 1-3 минуты)

### Если файл испорчен:
1. Проверь последний коммит на GitHub
2. Сравни размеры: должен быть > 30,000 байт
3. Если < 30,000 - файл повреждён, нужен откат
4. Используй предыдущий SHA для восстановления

---

## 📚 РЕФЕРЕНСЫ

**Рабочий пример:**
- Файл: `index.html`
- Секция: `<section id="videos" class="videos-section">`
- Блок карточек: `<div class="videos-grid">`

**Стили:**
- Файл: `css/style.css`
- Классы: `.video-card`, `.video-card.gold`, `.video-card.silver`

**Скрипты:**
- Метрика: Yandex.Metrika (событие VIDEO_CLICK)
- ID счётчика: 105507158

---

**Последнее обновление:** 26.11.2025, 17:15 MSK  
**Версия:** 1.0  
**Статус:** Проверено и работает  
**Критические изменения:** Добавлены все проверки безопасности